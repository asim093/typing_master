# Base URL where the character GLB model files are hosted (CDN/bucket).
# Leave empty for local dev — models are then served from public/ as usual.
# In production, point this at wherever the .glb files were uploaded, e.g.:
# VITE_ASSET_BASE_URL=https://your-bucket.example.com/models
VITE_ASSET_BASE_URL=
